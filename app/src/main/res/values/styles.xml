<resources>

    <style name="AppTheme" parent="AppTheme.Base" />

    <style name="SettingsSectionLabelStyle" parent="@style/MaterialSectionLabelStyle">
        <item name="android:layout_marginStart">@dimen/settings_label_start_margin</item>
        <item name="android:textSize">@dimen/big_text_size</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- commons' SettingsTextLabelStyle/SettingsSwitchStyle default to normal_text_size (14sp),
         which reads too small for this app's settings rows - bump both by 2sp -->
    <style name="SettingsTextLabelStyle" parent="@style/SettingsTextLabelStyle">
        <item name="android:textSize">16sp</item>
    </style>

    <style name="SettingsSwitchStyle" parent="@style/SettingsSwitchStyle">
        <item name="android:textSize">16sp</item>
    </style>

    <style name="LauncherTheme" parent="Theme.Material3.Dark.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowShowWallpaper">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>

        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:enforceNavigationBarContrast">false</item>
        <item name="android:enforceStatusBarContrast">false</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

</resources>
