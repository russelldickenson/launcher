<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/pill_menu_item"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?attr/selectableItemBackground"
    android:clickable="true"
    android:focusable="true"
    android:gravity="center_vertical"
    android:minHeight="@dimen/pill_menu_item_height"
    android:orientation="horizontal"
    android:paddingStart="@dimen/pill_menu_item_padding_horizontal"
    android:paddingEnd="@dimen/pill_menu_item_padding_horizontal">

    <ImageView
        android:id="@+id/pill_menu_icon"
        android:layout_width="@dimen/menu_icon_size"
        android:layout_height="@dimen/menu_icon_size"
        android:contentDescription="@null"
        tools:ignore="ContentDescription" />

    <TextView
        android:id="@+id/pill_menu_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/pill_menu_icon_padding"
        android:gravity="center_vertical"
        android:textAppearance="@style/TextAppearance.Launcher.PillPopupMenuItem"
        android:textColor="?attr/colorOnSurface" />

</LinearLayout>
